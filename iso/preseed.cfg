# Debian 12 Preseed for Nexus Core
# This file automates the Debian installation and runs the Nexus installer at the end.

# Localization
d-i debian-installer/locale string es_ES
d-i keyboard-configuration/xkb-keymap select es

# Network configuration
d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string nexus-core
d-i netcfg/get_domain string local

# Mirror settings
d-i mirror/country string manual
d-i mirror/http/hostname string deb.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string

# Account setup
d-i passwd/root-login boolean true
d-i passwd/make-user boolean false
d-i passwd/root-password password nexus
d-i passwd/root-password-again password nexus

# Clock and time zone setup
d-i clock-setup/utc boolean true
d-i time/zone string Europe/Madrid
d-i clock-setup/ntp boolean true

# Partitioning
d-i partman-auto/method string regular
d-i partman-auto/choose_recipe select atomic
d-i partman-partitioning/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true

# Package selection
tasksel tasksel/first multiselect standard, ssh-server
d-i pkgsel/include string curl git whiptail sudo
d-i pkgsel/upgrade select full-upgrade

# Boot loader installation
d-i grub-installer/only_debian boolean true
d-i grub-installer/with_other_os boolean true
d-i grub-installer/bootdev  string default

# Late command: Run the Nexus installer
d-i preseed/late_command string \
    in-target curl -o /root/nexus-installer.sh https://[TU-DOMINIO]/nexus-installer.sh; \
    in-target chmod +x /root/nexus-installer.sh; \
    in-target /root/nexus-installer.sh --auto
